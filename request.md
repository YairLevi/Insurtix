# User Request

Display a simple Angular page with minimal Tailwind CSS styling showing:

- An `<h1>` with the text **"Hello Angular!"**
- A counter variable displayed on the page
- A small button that increments the counter when clicked

Remove all unnecessary boilerplate files, unused styles, unneeded HTML/CSS snippets, and over-engineered code. Expect very minimal code.
